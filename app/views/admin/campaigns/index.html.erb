<h1>Campaigns</h1>

<table>
  <tr>
    <th>Group</th>
    <th>Value</th>
    <th>Element id</th>
    <th>Historic</th>
    <th>Newsletter</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_campaigns.each do |admin_campaign| %>
  <tr>
    <td><%= admin_campaign.kind %></td>
    <td><%= admin_campaign.value %></td>
    <td><%= admin_campaign.element_id %></td>
    <td><%= admin_campaign.historic %></td>
    <td><%= admin_campaign.newsletter.title %></td>
    <td><%= link_to 'Show', admin_campaign %></td>
    <td><%= link_to 'Edit', edit_admin_campaign_path(admin_campaign) %></td>
    <td><%= link_to 'Destroy', admin_campaign, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Campaign', new_admin_campaign_path %>
