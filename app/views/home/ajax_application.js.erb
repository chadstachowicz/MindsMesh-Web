if ($('#top_menu_left').length) {
  var s = "<%= j render '/partials/application/notifications' %>";
  $('#top_menu_left').append(s);
}

if ($('#top_menu_left').length) {
  var s = "<%= j render '/partials/application/messages' %>";
  $('#top_menu_left').append(s);
}



if ($('#top_menu_right').length) {
  var s = "<%= j render '/partials/application/myaccount' %>";
  $('#top_menu_right').append(s);
}

var s = "<%= j render '/partials/application/follow_recs' %>";
$('body').append(s);


<% if my_fb_friends_i_should_invite.any? %>

if ($('#sidebar_right').length) {
  var s = "<%= j render '/partials/application/fb_invites_rightbar' %>";
  $('#sidebar_right').append(s);
}


var s = "<%= j render '/partials/application/fb_invites' %>";
$('body').append(s);



if ($.cookie('suggest_follows') == "true") {
  var _callback = function() {
    $('#follow_recs_modal').modal();
    $.cookie('suggest_follows', 'false');
  }
  setTimeout(_callback, 1000);
} else if ($.cookie('suggest_invites') == "true") {
  var _callback2 = function() {
    $('#fb_friends_modal').modal();
    $.cookie('suggest_invites', 'false');
  }
  setTimeout(_callback2, 1000);
}

<% end %>